<UserControl x:Class="CashierTerminalPP.View.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:CashierTerminalPP.View"
             xmlns:viewmodels="clr-namespace:CashierTerminalPP.ViewModel"
             mc:Ignorable="d" Style="{StaticResource Base}"
             d:DesignHeight="450" d:DesignWidth="800">

    <!--#region UserControl Resources-->
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type viewmodels:CashierPageViewModel}">
            <views:CashierView/>
        </DataTemplate>
    </UserControl.Resources>
    <!--#endregion-->
    <Grid>
        <Button Content="Logga in" HorizontalAlignment="Center" VerticalAlignment="Center" Height="50" Width="100"
                Command="{Binding CashierPageCommand}"/>
    </Grid>
</UserControl>
